const CONFIG = {
    EXPRESS_PORT: 3000,
    SYSLOG_PORT: 514,
    CONSOLE_UPDATE_INTERVAL: 1000,
    ROUTER_CONFIG: 'routers.json',
    LOG_BASE_DIR: 'logs'  // Base directory for router logs
};

module.exports = CONFIG;
